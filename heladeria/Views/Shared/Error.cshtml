@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<div class="container">


    <h1 class="text-center text-danger" style="font-family: 'Poppins', sans-serif; font-weight: 600;">Error</h1>
    <h2 class="text-center text-danger" style="font-family: 'Poppins', sans-serif;">Un error ha ocurrido al procesar tu solicitud</h2>


    <div class="alert alert-danger" role="alert" style="background-color: #f8d7da; border-color: #f5c6cb; color: #721c24; font-family: 'Poppins', sans-serif; font-size: 1.2em;">
        @Model.message
    </div>

    @if (Model.ShowRequestId)
    {
        <div class="text-center mt-4" style="font-family: 'Poppins', sans-serif;">
            <p><strong>Request ID:</strong> <code style="background-color: #f5f5f5; color: #721c24;">@Model.RequestId</code></p>
        </div>
    }


    <div class="container mt-5" style="background-color: #f9f7f2; padding: 20px; border-radius: 10px; box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);">
        <h3 class="text-center" style="color: #8a4b2b;">Modo de Desarrollo</h3>
        <p style="color: #5a3e26; font-family: 'Poppins', sans-serif;">
            Cambiar al entorno de <strong>Desarrollo</strong> mostrará información más detallada sobre el error que ocurrió.
        </p>
        <p style="color: #5a3e26; font-family: 'Poppins', sans-serif;">
            <strong>El entorno de Desarrollo no debe habilitarse en aplicaciones desplegadas.</strong> Esto puede resultar en la visualización de información sensible de excepciones para los usuarios finales.
        </p>
        <p style="color: #5a3e26; font-family: 'Poppins', sans-serif;">
            Para depuración local, habilita el entorno <strong>Desarrollo</strong> estableciendo la variable de entorno <strong>ASPNETCORE_ENVIRONMENT</strong> a <strong>Development</strong> y reinicia la aplicación.
        </p>
    </div>


    <div class="text-center mt-5">
        <a href="/" class="btn btn-outline-warning btn-lg" style="font-weight: 600; padding: 12px 30px; border-radius: 8px; background-color: #f8d7da; color: #721c24; border: 1px solid #f5c6cb;">Volver al inicio</a>
    </div>

</div>
